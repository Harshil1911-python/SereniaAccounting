"""
SERENIA ACCOUNTING — config/settings/base.py
===============================================
Placeholder for shared settings. Currently, all settings
live in production.py for simplicity. For local development,
development.py overrides DEBUG and relaxes security settings.
"""

# Intentionally minimal — see production.py for the full configuration.
# This file exists so `config.settings.base` imports do not break
# if referenced by management commands or third-party apps.
